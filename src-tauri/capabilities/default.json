{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the tray app and its windows.",
  "windows": ["*"],
  "permissions": ["core:default"]
}
